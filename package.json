{
  "name": "etch-a-sketch",
  "version": "1.3.0",
  "description": "The Basic Childhood Sketch board on computer",
  "main": "index.js",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build src/index.html --out-dir docs --public-url ./",
    "deploy": "npm run build && git add . && git commit -a -m 'latest build' && git push origin gh-pages"
  },
  "keywords": [
    "etch a sketch",
    "sketch"
  ],
  "author": "raurosaur",
  "license": "MIT",
  "dependencies": {
    "dom-to-image": "^2.6.0"
  },
  "homepage": "https://raurosaur.github.io/etch-a-sketch",
  "devDependencies": {
    "cssnano": "^4.1.10",
    "gh-pages": "^3.1.0"
  }
}
