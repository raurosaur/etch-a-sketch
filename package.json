{
  "name": "etch-a-sketch",
  "version": "1.3.0",
  "description": "The Basic Childhood Sketch board on computer",
  "main": "index.js",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --out-dir build --public-url ./",
    "deploy": "git add build && git commit -m \"Build\" && git subtree push --prefix build origin gh-pages"
  },
  "keywords": [
    "etch a sketch",
    "sketch"
  ],
  "author": "raurosaur",
  "license": "MIT",
  "dependencies": {
    "dom-to-image": "^2.6.0"
  },
  "homepage": "https://raurosaur.github.io/etch-a-sketch",
  "devDependencies": {
    "cssnano": "^4.1.10"
  }
}
